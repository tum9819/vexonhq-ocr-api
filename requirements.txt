# Upper bounds keep a Coolify rebuild from silently pulling a breaking major
# (audit 2026-07-03). The ceilings are the guard; floors stay loose because
# the Docker image is python:3.10-slim — e.g. numpy>=2.4 needs Python 3.11+
# and broke the 2026-07-03 deploy. Keep floors 3.10-installable.
fastapi>=0.133,<1.0
uvicorn>=0.41,<1.0
pytesseract>=0.3.13,<0.4
opencv-python-headless>=4.13,<5
numpy>=1.26,<3
Pillow>=10,<13
pypdfium2>=4.30.0
pdfplumber>=0.11.0
openai>=1.40.0
supabase>=2.7.0
pydantic>=2.0.0
openpyxl>=3.1.0
pandas>=2.0.0
psycopg2-binary>=2.9.0
python-multipart>=0.0.6
apscheduler>=3.10.0
PyJWT>=2.4.0

cryptography>=3.4
PyNaCl>=1.5.0
psutil>=5.9.0
boto3>=1.26.0
sentry-sdk[fastapi]>=2.0.0
